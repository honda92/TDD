/* *  Money.h *  Money * *  Created by 本田 純子 on 11/08/04. *  Copyright 2011 nara-wu. All rights reserved. * */#include <typeinfo>class Dollar;class Franc;class Money {protected:	int amount;public:	virtual bool operator== (const Money& object) const	{		return (this->amount == object.amount) && (typeid(*this) == typeid(object));	}		static Dollar dollar(int amount);	static Franc franc(int amount);	Money* times (int multiplier);};class Dollar : public Money{	public:	Dollar(int amount);	Dollar(const Dollar& dollar);		Dollar* times(int multiplier);};class Franc : public Money{	public:	Franc(int amount);	Franc (const Franc& franc);		Franc* times(int multiplier);};