/* *  Money.h *  Money * *  Created by 本田 純子 on 11/08/04. *  Copyright 2011 nara-wu. All rights reserved. * */#include <iostream>#include <string>#include <typeinfo>using namespace std;class Dollar;class Franc;class Money {protected:	int amount;	string m_currency;public:	Money(int amount,const string& currency);		virtual bool operator== (const Money& object) const	{		return (this->amount == object.amount) && this->m_currency == object.m_currency;	}		static Dollar* dollar(int amount);	static Franc* franc(int amount);	Money* times (int multiplier);	string get_currency();};class Dollar : public Money{	public:	Dollar(int amount,const string& currency);};class Franc : public Money{	public:	Franc(int amount,const string& currency);};